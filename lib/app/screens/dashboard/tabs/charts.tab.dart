import 'package:flutter/material.dart';

class ChartsTab extends StatelessWidget {
  const ChartsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Charts"));
  }
}
